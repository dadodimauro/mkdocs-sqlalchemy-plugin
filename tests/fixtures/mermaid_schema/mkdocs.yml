site_name: Test SQLAlchemy Plugin
site_description: Testing the SQLAlchemy documentation plugin

theme:
  name: material

plugins:
  - mermaid2
  - sqlalchemy:
      base_class: "mermaid_schema.models.Base"
      app_path: "src"
      display:
        group_by_schema: true

nav:
  - Home: index.md